# --- المكتبات والاعتمادات (Dependencies) ---
/node_modules
/.pnp
.pnp.js
.pnp.loader.mjs

# --- ملفات البناء (Next.js build output) ---
/.next/
/out/
/build/
/dist/
.cache/

# --- ملفات الحماية والمتغيرات البيئية (Secrets & Env) ---
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local
.env.local

# --- ملفات حسابات الخدمة السرية (Sensitive Keys) ---
firebase-service-account.json
firebase-service-account-key.json
service-account.json
service-account-key.json
serviceAccountKey.json
temp_service_account.json
cors.json

# --- ملفات النظام وسجلات الأخطاء (System & Logs) ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.vscode/
.idea/

# --- Duplicated entries from user, cleaned up ---
# /.next/ (already listed)
# out/ (already listed as /out/)
# node_modules/ (already listed as /node_modules)
# .env*.local (already listed)
.firebase/